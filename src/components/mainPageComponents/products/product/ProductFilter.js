import { Category } from '@material-ui/icons'
import React from 'react'

const ProductFilter = ({categoryName}) => {
    return (
        <div>
            <h1>{categoryName}</h1>
        </div>
    )
}

export default ProductFilter
